#install shit
alias sai='sudo apt-get install'

#trolls
alias trollolol='cat ~/.troll'

#some more ls aliases
alias ll='ls -l'
alias la='ls -A'
alias l='ls -CF'

#common types on exit
alias exi='exit'
alias exity='exit'
alias :q='exit'

#sudo emacs as semacs
alias semacs='sudo emacs'

#calendar starts mondays
alias cal='ncal -M -C'

#common typo
alias eamcs='emacs'

#mkdir will now make all folders on a path if they don't exist
alias mkdir='mkdir -p'

#hoogle now in colour
alias hoogle='hoogle --c=TRUE'